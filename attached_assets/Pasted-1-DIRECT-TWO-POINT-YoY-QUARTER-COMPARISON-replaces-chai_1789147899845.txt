1. DIRECT TWO-POINT YoY QUARTER COMPARISON (replaces chain-linking entirely) - Remove all chain-linking, monthly link ratios, and compounding logic. The model compares exactly two quarters, four quarters apart. No intermediate periods are computed or used. - Matched set = rooms with a valid rate in ALL THREE months of the base quarter AND ALL THREE months of the comparison quarter. Months between the two quarters are IRRELEVANT and must not affect qualification. - Explicitly remove the full-window (12-month) survival requirement. A room that failed the gate, went offline, or changed payer in any month outside the two compared quarters is still eligible. - Room quarter rate = simple average of its three monthly rates in that quarter. No partial quarters. No month-weighting. 3. STRATIFIED WEIGHTING - Strata = unit type x care level x price band (existing planning bands; if none, quartiles of rate within unit type). - Compute the matched ratio INDEPENDENTLY within each stratum. - Aggregate strata with fixed weights. Renormalize within each stratum so it carries its full weight regardless of how many of its rooms matched. - Primary output uses ENDING-QUARTER stratum weights. Also compute and store the BASE-QUARTER weighted version as a secondary field. - Surface the spread between the two as the composition effect; it must reconcile to the mix effect in section 5. 4. FROZEN ELIGIBILITY - Adjudicate the outlier gate, base-rate exclusions, and payer filters ONCE per comparison, at the ending quarter. - Pull each matched room's ACTUAL recorded rate in the base quarter. Do not re-adjudicate, re-gate, impute, or substitute a stratum average on the historical side. Historical rates that would fail today's gate are used as-is. 10. REMOVED - Delete the chained index, the Fisher composite, and the separate backcast view. Section 1 now IS the backcast, computed once, and is the only growth engine. COVERAGE - Report match coverage per stratum as: matched rooms / rooms priced in the ending quarter, by count and by ending-quarter revenue share. - Suppress any stratum below 90% (null + reason code) and redistribute its weight proportionally across remaining strata in the same unit type, flagged in the output. VALIDATION - Report new coverage for AL, AL/MC, SL, VIL, HC against the old full-window figures (56/49/63/66/27) to confirm the recovery. - AL raw monthly rates 5802 -> 5805 must not produce a rate decline. - Assert rate effect + mix effect == raw change for every scope.