node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Data files are included for private repository sharing
# Note: Only share via private repositories to protect sensitive data

# Database backups
*.dump
*.sql

# Environment and secrets
.env
.env.*